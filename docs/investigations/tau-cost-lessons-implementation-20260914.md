# Applying measured cost lessons to Tau

User scope: implement compression/focused-review/stable-prefix lessons; retain routing policy and establish actual routing dollar savings later. Effectiveness does not gate token/cost analysis. Memory used: tau-cost-efficiency-lessons.md and tau-routing-contract.md (home/global local runtime workflow).

Review payloads now abbreviate long raw tool evidence (>4096characters) with a stable512character preview and CCR handle. Focused source search uses the intention outcome or latest user request, without another model call. It returns up to8items, capped at2048characters per excerpt, marked partial; original content remains retrievable. Existing compressed blocks remain unchanged. Instructions, user messages/corrections, tool IDs/errors, candidate references and custom memory remain. Runtime usage/provider/model/timestamp fields are removed from review evidence. Stable working instructions/capabilities precede history; changing intention and focused excerpts are at the tail. Storage/search failure preserves full input evidence. Worker messages/session history are not mutated. This is bounded evidence projection, not proof of universally optimal query selection or live provider cache hits.

Replay of two captured full-source review inputs:23005→3289 and23014→3298characters (~85.7%reduction). Original CCR source and503 evidence retained, worker context unchanged. Artifacts: ~/.codex/evidence/tau-cost-lessons-implementation-20260914/projection-replay.json and replay.py. Character reduction is not billed-token/dollar reduction.

Every review/intention provider invocation now records a session event before yielding its terminal provider event. Exception/cancellation fallback records an invocation with unknown usage. Completion records mark per_invocation accounting so stats do not count the same call twice. Legacy completion events remain supported; historical failed runs without call records are disclosed. Returned total-token residuals are shown as unclassified, not silently omitted or assigned output pricing.

Router selections and auxiliary invocation records capture configured per-million-token rates (only explicitly set fields). Stats price only matching provider/model identities with complete used-category rates and classifiable usage. Partial/missing pricing or usage remains unpriced. Display calls these configured-rate estimates, not bills, subscription charges or routing savings. Removed unfinished hard-coded model prices; no invented cache-write charges or all-Astra counterfactual. Actual routing dollar savings remain unproven.

Also completed the existing parallel-tool cancellation edit: parent cancellation cancels and drains child tool tasks before returning. Focused regression observes both asynchronous cleanup files already persisted when parent cancellation completes.

Validation:55focused source checks (review/context/stats/router, cancellation, CCR outbound PII) and installed-runtime checks. Controlled provider responses verify plumbing and failure accounting, not model effectiveness. Read-only critic independently found terminal-event ordering, stale-price identity and unknown-rate/usage gaps; focused regressions now cover them and final review found no blocker. A broader incidental test run found two existing steering failures; they are documented in the evidence directory and are outside these cost changes.
